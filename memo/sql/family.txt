

CREATE TABLE FAMILY (
  mail VARCHAR(64) NOT NULL AUTO_INCREMENT,
  family_name VARCHAR(64) NOT NULL,
  family_pass VARCHAR(64) NOT NULL,
  family_salt VARCHAR(64),
  family_date VARCHAR(32),
  family_update TIMESTAMP,
  PRIMARY KEY (mail)
);
